/// Defines the visual style of the button.
enum AppButtonVariant { solid, outline, ghost, soft, link }

/// Defines the size constraints common to all buttons.
enum AppButtonSize { xl, lg, md, sm, xs }

/// Defines common color schemes in the UI Kit.
enum AppButtonColor {
  primary,
  secondary,
  success,
  warning,
  danger,
  info,
  light,
  dark,
}
