import 'app_base_widget.dart';

/// A wrapper for stateless widgets that provides foundation accessors.
abstract class AppStatelessWrapper extends AppBaseWidget {
  const AppStatelessWrapper({super.key});
}
