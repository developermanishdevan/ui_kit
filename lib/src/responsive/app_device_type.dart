/// Represents the general device types based on screen width breakpoints.
enum AppDeviceType {
  /// Extra small and small devices (phones).
  mobile,

  /// Medium devices (tablets).
  tablet,

  /// Large devices (desktops).
  desktop,

  /// Extra large screens and beyond.
  largeDesktop,
}
